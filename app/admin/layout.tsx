/**
 * Layout para as páginas de /admin.
 * Não usa a sidebar global — renderiza o conteúdo em tela cheia.
 */
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
